{% extends "PlanningBundle::layout.html.twig" %}

{% block title %}PlanningBundle:Location:addSite{% endblock %}

{% form_theme form 'bootstrap_3_horizontal_layout.html.twig' %}

{% block content %}

    <h1>Modification du site : {{ site.name }} <small><a class="btn btn-primary btn-xs" href="{{ path('add_room', {site:site.id}) }}">Ajouter une salle</a></small></h1>

    {{ form(form) }}

    <a class="btn btn-primary btn-xs" href="{{ path("delete_site",{id:site.id})  }}">Supprimer un site</a>

{% endblock %}